<?php

	//	1 - Query to Database for user
	//	2 - Using query function to send query request to DB
	//	3 - Fetching query using mysqli fetch (not in a custom function)
	//	3 - Echoing the username from the fetched array variable (in pre)

	/* $query = "SELECT * FROM users WHERE id='1' ";
	$result = $database_handler->query($query);
	$user_found = mysqli_fetch_array($result);
	echo "<pre>".print_r($user_found['username'],true)."</pre>"; */

?>
